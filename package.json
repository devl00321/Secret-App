{
  "name": "love-app",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspace mobile",
    "typecheck": "npm run typecheck --workspace mobile && npm run typecheck --workspace server && npm run typecheck --workspace shared",
    "test": "npm run lint && npm run typecheck",
    "dev:mobile": "npm run start --workspace mobile",
    "dev:server": "npm run dev --workspace server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "overrides": {
    "xmldom": "npm:@xmldom/xmldom@^0.9.10",
    "uuid": "^14.0.0",
    "postcss": "^8.5.10",
    "@tootallnate/once": "^3.0.1",
    "ajv": "^8.12.0",
    "expo-constants": "~18.0.13",
    "expo-firebase-core": {
      "expo-constants": "~18.0.13"
    }
  },
  "devDependencies": {
    "react-native-worklets": "^0.5.1"
  }
}
